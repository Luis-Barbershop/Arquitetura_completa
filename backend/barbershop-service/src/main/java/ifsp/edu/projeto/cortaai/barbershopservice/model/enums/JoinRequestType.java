package ifsp.edu.projeto.cortaai.barbershopservice.model.enums;

public enum JoinRequestType {
    /** Fluxo antigo: barbeiro solicita entrada via CNPJ. */
    JOIN,
    /** Fluxo novo: owner convida barbeiro via CPF. */
    INVITE
}
